import 'package:flutter/material.dart';

class ReserveRoomPage extends StatelessWidget {
  const ReserveRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          "Reserve here. ill implement this when i think of what ill add here",
          style: TextStyle(fontSize: 30, fontWeight: FontWeight.w600),
        ),
      ),
    );
  }
}
